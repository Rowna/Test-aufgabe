import '../styles/app.scss';

var slider = tns({
    container: '.my-slider',
    slideBy: 1,
    // controlsText: ["prev", "next"],
    controlsPositon: 'bottom',
    navPosition: 'bottom',
    controlsContainer: '#custom-scroll'
});

